:bdg-primary:`image` ``Image``

:bdg-success:`align` ``String``

Default: ``NONE``

**Alignment Types**

- ``NONE`` No coordinate offset, world coordinates match the image coordinates.
  The top left of images is considered (0,0).
- ``CENTER`` Offsets the image such that the center of the image will be at (0,0).

